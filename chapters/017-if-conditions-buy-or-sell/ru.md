# День 17: Условия if — покупать или продавать?

## Аналогия из трейдинга

Представь: ты смотришь на график и принимаешь решение. **Если** цена ниже скользящей средней — покупаем. **Если** RSI выше 70 — перекуплено, не входим. Каждое торговое решение — это условие. В программировании условия работают точно так же через оператор `if`.

## Базовый синтаксис if

```rust
fn main() {
    let btc_price = 42000.0;
    let buy_level = 40000.0;

    if btc_price < buy_level {
        println!("Цена ниже уровня покупки — ПОКУПАЕМ!");
    }
}
```

**Важно:** Условие должно возвращать `bool` (true или false). Rust не делает неявных преобразований типов.

## Операторы сравнения

```rust
fn main() {
    let current_price = 42000.0;
    let entry_price = 41500.0;
    let target_price = 43000.0;
    let stop_loss = 41000.0;

    // Равенство и неравенство
    if current_price == entry_price {
        println!("Цена на уровне входа");
    }

    if current_price != entry_price {
        println!("Цена изменилась с момента входа");
    }

    // Больше / меньше
    if current_price > entry_price {
        println!("Мы в прибыли!");
    }

    if current_price < stop_loss {
        println!("СТОП-ЛОСС сработал!");
    }

    // Больше или равно / меньше или равно
    if current_price >= target_price {
        println!("Тейк-профит достигнут!");
    }

    if current_price <= stop_loss {
        println!("Достигли или пробили стоп-лосс");
    }
}
```

## Определяем прибыльность сделки

```rust
fn main() {
    let entry_price = 42000.0;
    let exit_price = 43500.0;
    let quantity = 0.5;

    let pnl = (exit_price - entry_price) * quantity;

    if pnl > 0.0 {
        println!("Прибыльная сделка! PnL: ${:.2}", pnl);
    }

    if pnl < 0.0 {
        println!("Убыточная сделка. PnL: ${:.2}", pnl);
    }

    if pnl == 0.0 {
        println!("Безубыток");
    }
}
```

## Проверка рыночных условий

```rust
fn main() {
    let market_open = true;
    let has_balance = true;
    let price_valid = true;

    if market_open {
        println!("Рынок открыт, можем торговать");
    }

    if !market_open {
        println!("Рынок закрыт, ждём открытия");
    }

    // Прямая проверка булевых переменных
    if has_balance {
        println!("Баланс достаточный");
    }

    if price_valid {
        println!("Цена валидна");
    }
}
```

## Логические операторы

### AND (&&) — все условия должны быть истинны

```rust
fn main() {
    let price = 42000.0;
    let rsi = 35.0;
    let volume = 1500.0;
    let min_volume = 1000.0;

    // Покупаем только если ВСЕ условия выполнены
    if price < 43000.0 && rsi < 40.0 && volume > min_volume {
        println!("Все условия для покупки соблюдены — ПОКУПАЕМ!");
    }
}
```

### OR (||) — хотя бы одно условие истинно

```rust
fn main() {
    let rsi = 75.0;
    let price_at_resistance = true;
    let bearish_divergence = false;

    // Сигнал на продажу если ХОТЯ БЫ ОДНО условие выполнено
    if rsi > 70.0 || price_at_resistance || bearish_divergence {
        println!("Есть сигнал на продажу!");
    }
}
```

### NOT (!) — отрицание

```rust
fn main() {
    let is_weekend = false;
    let market_holiday = false;

    if !is_weekend && !market_holiday {
        println!("Торговый день, можно работать");
    }
}
```

## Комбинирование условий

```rust
fn main() {
    let price = 42000.0;
    let sma_20 = 41500.0;
    let sma_50 = 41000.0;
    let rsi = 45.0;
    let volume = 2000.0;
    let avg_volume = 1500.0;

    // Сложное условие для входа в лонг
    let price_above_sma = price > sma_20 && price > sma_50;
    let rsi_neutral = rsi > 40.0 && rsi < 60.0;
    let volume_confirmed = volume > avg_volume;

    if price_above_sma && rsi_neutral && volume_confirmed {
        println!("Сильный сигнал на покупку!");
        println!("Цена выше SMA20 и SMA50");
        println!("RSI в нейтральной зоне");
        println!("Объём подтверждает движение");
    }
}
```

## Валидация ордера

```rust
fn main() {
    let order_price = 42000.0;
    let order_quantity = 0.5;
    let balance = 25000.0;
    let min_order_size = 0.001;
    let max_order_size = 10.0;

    let order_value = order_price * order_quantity;

    // Проверяем валидность ордера
    if order_price > 0.0 {
        println!("✓ Цена положительная");
    }

    if order_quantity >= min_order_size && order_quantity <= max_order_size {
        println!("✓ Количество в допустимом диапазоне");
    }

    if order_value <= balance {
        println!("✓ Достаточно средств");
    }

    // Полная проверка
    if order_price > 0.0
        && order_quantity >= min_order_size
        && order_quantity <= max_order_size
        && order_value <= balance
    {
        println!("Ордер валиден — можно отправлять!");
    }
}
```

## Проверка рисков

```rust
fn main() {
    let position_size = 5000.0;
    let portfolio_value = 100000.0;
    let max_position_percent = 5.0;
    let current_drawdown = 8.0;
    let max_drawdown = 10.0;

    let position_percent = (position_size / portfolio_value) * 100.0;

    // Проверка размера позиции
    if position_percent > max_position_percent {
        println!("⚠ Позиция слишком большая: {:.1}% > {:.1}%",
                 position_percent, max_position_percent);
    }

    // Проверка просадки
    if current_drawdown > max_drawdown {
        println!("🛑 Превышена максимальная просадка! Торговля остановлена.");
    }

    // Можно ли торговать?
    if position_percent <= max_position_percent && current_drawdown <= max_drawdown {
        println!("✓ Риски в норме, можно торговать");
    }
}
```

## Практический пример: Сигнал на вход

```rust
fn main() {
    // Текущее состояние рынка
    let price = 42000.0;
    let sma_20 = 41500.0;
    let rsi = 35.0;
    let macd_histogram = 50.0;
    let volume = 2500.0;
    let avg_volume = 2000.0;

    println!("=== Анализ рынка BTC/USDT ===");
    println!("Цена: ${}", price);
    println!();

    // Проверяем каждое условие отдельно
    let trend_up = price > sma_20;
    let oversold = rsi < 40.0;
    let macd_bullish = macd_histogram > 0.0;
    let volume_high = volume > avg_volume;

    println!("Проверка условий:");

    if trend_up {
        println!("✓ Тренд восходящий (цена > SMA20)");
    }

    if oversold {
        println!("✓ RSI показывает перепроданность");
    }

    if macd_bullish {
        println!("✓ MACD бычий");
    }

    if volume_high {
        println!("✓ Объём выше среднего");
    }

    println!();

    // Финальное решение
    if trend_up && oversold && macd_bullish && volume_high {
        println!("🚀 СИГНАЛ: ПОКУПКА!");
    }
}
```

## if как выражение

В Rust `if` может возвращать значение:

```rust
fn main() {
    let pnl = 750.0;

    // if как выражение
    let status = if pnl > 0.0 {
        "PROFIT"
    } else {
        "LOSS"
    };

    println!("Статус сделки: {}", status);

    // Расчёт комиссии на основе условия
    let trade_volume = 50000.0;
    let fee_percent = if trade_volume > 100000.0 {
        0.05  // VIP комиссия
    } else {
        0.1   // Стандартная комиссия
    };

    let fee = trade_volume * (fee_percent / 100.0);
    println!("Комиссия: ${:.2} ({:.2}%)", fee, fee_percent);
}
```

**Важно:** При использовании `if` как выражения, оба блока должны возвращать один тип!

## Определение направления сделки

```rust
fn main() {
    let entry_price = 42000.0;
    let exit_price = 43500.0;

    let direction = if exit_price > entry_price {
        "LONG"
    } else {
        "SHORT"
    };

    let profit = (exit_price - entry_price).abs();

    println!("Направление: {}", direction);
    println!("Прибыль: ${:.2}", profit);
}
```

## Вложенные условия

```rust
fn main() {
    let balance = 10000.0;
    let min_balance = 1000.0;
    let price = 42000.0;
    let quantity = 0.1;
    let order_value = price * quantity;

    if balance >= min_balance {
        println!("Баланс достаточный для торговли");

        if order_value <= balance {
            println!("Ордер может быть исполнен");

            if quantity > 0.0 {
                println!("Количество валидно");
                println!("✓ Все проверки пройдены!");
            }
        }
    }
}
```

## Что мы узнали

| Оператор | Описание | Пример |
|----------|----------|--------|
| `==` | Равно | `price == target` |
| `!=` | Не равно | `price != entry` |
| `>` | Больше | `price > sma` |
| `<` | Меньше | `rsi < 30` |
| `>=` | Больше или равно | `volume >= min_volume` |
| `<=` | Меньше или равно | `drawdown <= max_drawdown` |
| `&&` | Логическое И | `a > 0 && b > 0` |
| `\|\|` | Логическое ИЛИ | `rsi > 70 \|\| rsi < 30` |
| `!` | Логическое НЕ | `!market_closed` |

## Домашнее задание

1. Напиши программу, которая проверяет, является ли цена входа выгодной (ниже SMA и RSI < 40)

2. Создай валидатор ордера, проверяющий:
   - Цена > 0
   - Количество > 0 и < максимума
   - Достаточно баланса
   - Рынок открыт

3. Реализуй проверку рисков:
   - Размер позиции не превышает 5% от портфеля
   - Просадка не превышает лимит
   - Корреляция с существующими позициями ниже 0.8

4. Напиши функцию, которая возвращает размер позиции на основе условий риска (используй `if` как выражение)

## Навигация

[← Предыдущий день](../016-comments-trading-logic/ru.md) | [Следующий день →](../018-else-market-scenarios/ru.md)
