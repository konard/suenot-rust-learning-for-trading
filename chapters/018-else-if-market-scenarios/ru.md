# День 18: else и else if — три сценария рынка

## Аналогия из трейдинга

Рынок редко бывает "просто бычьим" или "просто медвежьим". Обычно есть **три сценария**: рост, падение и боковик. Твоя торговая стратегия должна учитывать все три варианта:
- Цена выше SMA → **покупаем**
- Цена ниже SMA → **продаём**
- Цена около SMA → **ждём**

В Rust для этого используются `if`, `else if` и `else`.

## Базовый синтаксис else

```rust
fn main() {
    let price = 42500.0;
    let entry_price = 42000.0;

    if price > entry_price {
        println!("Позиция в прибыли!");
    } else {
        println!("Позиция в убытке или безубытке");
    }
}
```

**Важно:** `else` — это "всё остальное". Он выполняется, если условие `if` ложно.

## else if — множественные условия

```rust
fn main() {
    let price_change = 2.5; // процент изменения цены

    if price_change > 5.0 {
        println!("Сильный рост! Возможно, стоит зафиксировать прибыль");
    } else if price_change > 0.0 {
        println!("Умеренный рост. Держим позицию");
    } else if price_change > -5.0 {
        println!("Умеренное падение. Анализируем ситуацию");
    } else {
        println!("Сильное падение! Проверяем стоп-лосс");
    }
}
```

## Три сценария рынка: Buy / Sell / Hold

```rust
fn main() {
    let current_price = 42500.0;
    let sma_20 = 42000.0;
    let threshold = 100.0; // порог для определения "около"

    if current_price > sma_20 + threshold {
        println!("📈 SIGNAL: BUY — цена выше SMA");
    } else if current_price < sma_20 - threshold {
        println!("📉 SIGNAL: SELL — цена ниже SMA");
    } else {
        println!("⏸️ SIGNAL: HOLD — цена около SMA, ждём");
    }
}
```

## Анализ позиции: прибыль, убыток, безубыток

```rust
fn main() {
    let entry_price = 42000.0;
    let current_price = 42000.0;
    let quantity = 0.5;

    let pnl = (current_price - entry_price) * quantity;

    if pnl > 0.0 {
        println!("✅ Прибыль: ${:.2}", pnl);
    } else if pnl < 0.0 {
        println!("❌ Убыток: ${:.2}", pnl.abs());
    } else {
        println!("➖ Безубыток");
    }
}
```

## Классификация волатильности

```rust
fn main() {
    let daily_range_percent = 3.2;

    let volatility = if daily_range_percent < 1.0 {
        "низкая"
    } else if daily_range_percent < 3.0 {
        "средняя"
    } else if daily_range_percent < 5.0 {
        "высокая"
    } else {
        "экстремальная"
    };

    println!("Волатильность: {}", volatility);
}
```

**Обрати внимание:** `if`/`else` — это выражение! Можно присвоить результат переменной.

## Проверка ордера на валидность

```rust
fn main() {
    let balance = 10000.0;
    let order_price = 42000.0;
    let order_quantity = 0.3;
    let order_value = order_price * order_quantity;

    if order_price <= 0.0 {
        println!("❌ Ошибка: цена должна быть положительной");
    } else if order_quantity <= 0.0 {
        println!("❌ Ошибка: количество должно быть положительным");
    } else if order_value > balance {
        println!("❌ Ошибка: недостаточно средств (нужно ${:.2}, есть ${:.2})",
                 order_value, balance);
    } else {
        println!("✅ Ордер валиден: {} BTC по ${:.2}", order_quantity, order_price);
    }
}
```

## RSI сигналы: перекупленность / перепроданность / нейтрально

```rust
fn main() {
    let rsi = 72.5;

    let signal = if rsi >= 70.0 {
        ("OVERBOUGHT", "Возможна коррекция вниз")
    } else if rsi <= 30.0 {
        ("OVERSOLD", "Возможен отскок вверх")
    } else if rsi >= 50.0 {
        ("BULLISH", "Бычий тренд")
    } else {
        ("BEARISH", "Медвежий тренд")
    };

    println!("RSI: {:.1} — {} ({})", rsi, signal.0, signal.1);
}
```

## Управление позицией на основе PnL

```rust
fn main() {
    let entry = 42000.0;
    let current = 43500.0;
    let stop_loss = 41000.0;
    let take_profit = 45000.0;

    if current <= stop_loss {
        println!("🛑 STOP LOSS сработал! Закрываем позицию с убытком");
    } else if current >= take_profit {
        println!("🎯 TAKE PROFIT достигнут! Закрываем позицию с прибылью");
    } else if current > entry {
        let profit_percent = ((current - entry) / entry) * 100.0;
        println!("📈 В прибыли: +{:.2}%. Позиция открыта", profit_percent);
    } else if current < entry {
        let loss_percent = ((entry - current) / entry) * 100.0;
        println!("📉 В убытке: -{:.2}%. Позиция открыта", loss_percent);
    } else {
        println!("➖ На точке входа. Позиция открыта");
    }
}
```

## Определение тренда по нескольким SMA

```rust
fn main() {
    let price = 43000.0;
    let sma_20 = 42500.0;
    let sma_50 = 42000.0;
    let sma_200 = 40000.0;

    if price > sma_20 && sma_20 > sma_50 && sma_50 > sma_200 {
        println!("🚀 Сильный восходящий тренд (все SMA выстроились)");
    } else if price > sma_200 {
        println!("📈 Общий тренд восходящий");
    } else if price < sma_20 && sma_20 < sma_50 && sma_50 < sma_200 {
        println!("💥 Сильный нисходящий тренд (все SMA выстроились вниз)");
    } else if price < sma_200 {
        println!("📉 Общий тренд нисходящий");
    } else {
        println!("↔️ Боковой тренд или неопределённость");
    }
}
```

## Функция определения торгового сигнала

```rust
fn main() {
    let signals = [
        (42500.0, 42000.0, 45.0),  // price, sma, rsi
        (41500.0, 42000.0, 28.0),
        (42000.0, 42000.0, 50.0),
        (43000.0, 42000.0, 75.0),
    ];

    for (price, sma, rsi) in signals {
        let signal = get_trading_signal(price, sma, rsi);
        println!("Price: {}, SMA: {}, RSI: {:.0} → {}", price, sma, rsi, signal);
    }
}

fn get_trading_signal(price: f64, sma: f64, rsi: f64) -> &'static str {
    if rsi >= 70.0 {
        "SELL (RSI перекуплен)"
    } else if rsi <= 30.0 {
        "BUY (RSI перепродан)"
    } else if price > sma * 1.02 {
        "BUY (цена выше SMA на 2%+)"
    } else if price < sma * 0.98 {
        "SELL (цена ниже SMA на 2%+)"
    } else {
        "HOLD (нет чёткого сигнала)"
    }
}
```

## Оценка риска сделки

```rust
fn main() {
    let account_balance = 50000.0;
    let position_size = 15000.0;
    let risk_percent = (position_size / account_balance) * 100.0;

    let risk_level = if risk_percent > 20.0 {
        "КРИТИЧЕСКИЙ"
    } else if risk_percent > 10.0 {
        "ВЫСОКИЙ"
    } else if risk_percent > 5.0 {
        "СРЕДНИЙ"
    } else if risk_percent > 2.0 {
        "НИЗКИЙ"
    } else {
        "МИНИМАЛЬНЫЙ"
    };

    println!("Размер позиции: ${:.0} ({:.1}% от баланса)", position_size, risk_percent);
    println!("Уровень риска: {}", risk_level);

    if risk_percent > 10.0 {
        println!("⚠️ Рекомендация: уменьшить размер позиции!");
    }
}
```

## Вложенные условия

```rust
fn main() {
    let is_market_open = true;
    let has_signal = true;
    let has_balance = true;
    let risk_approved = true;

    if is_market_open {
        if has_signal {
            if has_balance {
                if risk_approved {
                    println!("✅ Все условия выполнены — открываем сделку!");
                } else {
                    println!("❌ Риск превышает лимит");
                }
            } else {
                println!("❌ Недостаточно средств");
            }
        } else {
            println!("⏸️ Нет торгового сигнала");
        }
    } else {
        println!("🔒 Рынок закрыт");
    }
}
```

**Лучше избегать глубокой вложенности!** Используй ранний выход:

```rust
fn can_open_trade(market_open: bool, signal: bool, balance: bool, risk_ok: bool) -> bool {
    if !market_open {
        println!("🔒 Рынок закрыт");
        return false;
    }
    if !signal {
        println!("⏸️ Нет сигнала");
        return false;
    }
    if !balance {
        println!("❌ Нет баланса");
        return false;
    }
    if !risk_ok {
        println!("❌ Риск превышен");
        return false;
    }

    println!("✅ Можно открывать сделку");
    true
}

fn main() {
    can_open_trade(true, true, true, true);
    can_open_trade(true, true, false, true);
}
```

## Что мы узнали

| Конструкция | Когда использовать |
|-------------|-------------------|
| `if` | Одно условие |
| `if-else` | Два варианта (да/нет) |
| `if-else if-else` | Три и более варианта |
| Вложенные `if` | Зависимые условия (лучше избегать) |
| `if` как выражение | Когда нужно вернуть значение |

## Упражнения

### Упражнение 1: Классификация сделки
Напиши код, который классифицирует сделку по результату:
- Убыток более 10% → "Катастрофа"
- Убыток 5-10% → "Большой убыток"
- Убыток до 5% → "Небольшой убыток"
- Прибыль до 5% → "Небольшая прибыль"
- Прибыль 5-10% → "Хорошая прибыль"
- Прибыль более 10% → "Отличная сделка"

### Упражнение 2: Время торговой сессии
По часу (0-23) определи торговую сессию:
- 0-7 (UTC) → Азиатская сессия
- 8-12 (UTC) → Европейская сессия
- 13-21 (UTC) → Американская сессия
- 22-23 (UTC) → Переходный период

### Упражнение 3: Рекомендация по размеру позиции
На основе волатильности и тренда дай рекомендацию:
- Высокая волатильность + нисходящий тренд → "Минимальный размер или не торговать"
- Высокая волатильность + восходящий тренд → "Уменьшенный размер"
- Низкая волатильность + любой тренд → "Стандартный размер"

### Упражнение 4: Многоуровневый стоп-лосс
Реализуй trailing stop с тремя уровнями:
- Прибыль 5%+ → подтянуть стоп на уровень безубытка
- Прибыль 10%+ → подтянуть стоп на +5%
- Прибыль 15%+ → подтянуть стоп на +10%

## Домашнее задание

1. **Создай функцию `analyze_market_condition`**, которая принимает цену, SMA-20, SMA-50 и объём, и возвращает строку с детальным анализом рынка (тренд, сила тренда, рекомендация).

2. **Реализуй систему риск-менеджмента**, которая на основе:
   - Баланса счёта
   - Текущих открытых позиций
   - Запрашиваемого размера новой позиции

   Выдаёт решение: "Разрешено", "Разрешено с уменьшением", "Запрещено" и объяснение.

3. **Создай функцию классификации ордера** по типу и условиям:
   - Рыночный / лимитный / стоп
   - Покупка / продажа
   - Срочность: немедленно / в течение дня / GTC

   Функция должна вернуть приоритет исполнения (1-10) и рекомендации.

4. **Напиши анализатор свечи** (open, high, low, close), который определяет:
   - Тип свечи (бычья/медвежья/доджи)
   - Размер тела (большое/среднее/малое)
   - Наличие теней (верхняя/нижняя/обе/нет)
   - Паттерн (молот, повешенный, волчок и т.д.)

## Навигация

[← Предыдущий день](../017-if-price-check/ru.md) | [Следующий день →](../019-match-order-types/ru.md)
