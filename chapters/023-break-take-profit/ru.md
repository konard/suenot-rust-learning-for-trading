# День 23: break — Выход по Take Profit

## Аналогия из трейдинга

Представь, что ты мониторишь цену актива в цикле, ожидая момента для закрытия позиции. Когда цена достигает твоего **Take Profit** уровня — ты немедленно выходишь из сделки, не дожидаясь конца торговой сессии. Именно так работает `break` в Rust: он позволяет **немедленно выйти из цикла**, когда условие выполнено.

## Базовый синтаксис break

```rust
fn main() {
    let prices = [42000.0, 42500.0, 43000.0, 43500.0, 44000.0];
    let take_profit = 43200.0;

    for price in prices {
        println!("Текущая цена: ${:.2}", price);

        if price >= take_profit {
            println!("🎯 Take Profit достигнут! Закрываем позицию.");
            break;  // Выходим из цикла
        }
    }

    println!("Мониторинг завершён");
}
```

**Вывод:**
```
Текущая цена: $42000.00
Текущая цена: $42500.00
Текущая цена: $43000.00
Текущая цена: $43500.00
🎯 Take Profit достигнут! Закрываем позицию.
Мониторинг завершён
```

## break в цикле loop

```rust
fn main() {
    let mut price = 42000.0;
    let take_profit = 43500.0;
    let stop_loss = 41000.0;

    loop {
        // Симуляция изменения цены
        price += (price * 0.005) * if price > 42500.0 { 1.0 } else { -0.5 };

        println!("Цена: ${:.2}", price);

        if price >= take_profit {
            println!("✅ Take Profit! Прибыль зафиксирована.");
            break;
        }

        if price <= stop_loss {
            println!("❌ Stop Loss! Убыток ограничен.");
            break;
        }
    }
}
```

## break с возвратом значения

В Rust `loop` может возвращать значение через `break`:

```rust
fn main() {
    let prices = [41500.0, 42000.0, 42800.0, 43200.0, 43000.0];
    let entry_price = 42000.0;

    let exit_price = loop {
        // В реальности здесь был бы поток данных
        static mut INDEX: usize = 0;

        let price = unsafe {
            let p = prices.get(INDEX).copied();
            INDEX += 1;
            p
        };

        match price {
            Some(p) if p >= entry_price * 1.03 => {
                println!("Take Profit на ${:.2}", p);
                break p;  // Возвращаем цену выхода
            }
            Some(p) if p <= entry_price * 0.98 => {
                println!("Stop Loss на ${:.2}", p);
                break p;
            }
            Some(p) => println!("Ждём... цена ${:.2}", p),
            None => {
                println!("Данные закончились");
                break entry_price;  // Выходим по входной цене
            }
        }
    };

    let pnl = exit_price - entry_price;
    println!("PnL: ${:.2}", pnl);
}
```

## Более безопасный пример с возвратом значения

```rust
fn main() {
    let orders = [
        ("BTC", 100.0),
        ("ETH", 50.0),
        ("INVALID", -10.0),  // Некорректный ордер
        ("SOL", 25.0),
    ];

    let result = loop {
        let mut found_invalid = false;
        let mut invalid_ticker = "";

        for (ticker, amount) in &orders {
            if *amount < 0.0 {
                found_invalid = true;
                invalid_ticker = ticker;
                break;  // Внутренний break
            }
        }

        if found_invalid {
            break format!("Ошибка: некорректный ордер {}", invalid_ticker);
        } else {
            break String::from("Все ордера валидны");
        }
    };

    println!("{}", result);
}
```

## Поиск первого прибыльного сигнала

```rust
fn main() {
    let signals = [
        ("BTC", -2.5),   // Убыточный
        ("ETH", -1.2),   // Убыточный
        ("SOL", 3.8),    // Прибыльный!
        ("ADA", 1.5),    // Прибыльный
    ];

    let mut found_signal: Option<(&str, f64)> = None;

    for (ticker, profit_pct) in signals {
        if profit_pct > 0.0 {
            found_signal = Some((ticker, profit_pct));
            break;  // Нашли — выходим
        }
        println!("Пропускаем {}: убыток {:.1}%", ticker, profit_pct);
    }

    match found_signal {
        Some((ticker, pct)) => {
            println!("Первый прибыльный сигнал: {} (+{:.1}%)", ticker, pct);
        }
        None => println!("Прибыльных сигналов нет"),
    }
}
```

## Мониторинг до достижения цели

```rust
fn main() {
    let target_balance = 11000.0;
    let mut balance = 10000.0;
    let mut trades = 0;

    let trade_results = [150.0, -50.0, 200.0, -30.0, 300.0, 100.0, -80.0, 500.0];

    for pnl in trade_results {
        trades += 1;
        balance += pnl;

        println!("Сделка #{}: PnL ${:.2}, Баланс: ${:.2}", trades, pnl, balance);

        if balance >= target_balance {
            println!("🎉 Цель достигнута за {} сделок!", trades);
            break;
        }
    }

    if balance < target_balance {
        println!("Цель не достигнута. Финальный баланс: ${:.2}", balance);
    }
}
```

## Валидация ордеров с ранним выходом

```rust
fn main() {
    let order_price = 42000.0;
    let order_quantity = 0.5;
    let balance = 20000.0;

    let validation_passed = validate_order(order_price, order_quantity, balance);

    if validation_passed {
        println!("✅ Ордер принят к исполнению");
    } else {
        println!("❌ Ордер отклонён");
    }
}

fn validate_order(price: f64, quantity: f64, balance: f64) -> bool {
    let checks = [
        (price > 0.0, "Цена должна быть положительной"),
        (quantity > 0.0, "Количество должно быть положительным"),
        (price * quantity <= balance, "Недостаточно средств"),
        (quantity >= 0.001, "Минимальный размер ордера 0.001"),
    ];

    for (condition, error_msg) in checks {
        if !condition {
            println!("Ошибка валидации: {}", error_msg);
            return false;  // Ранний выход из функции (не break!)
        }
    }

    true
}
```

## break vs return

```rust
fn main() {
    let result = process_trades();
    println!("Результат: {}", result);
}

fn process_trades() -> String {
    let trades = [100.0, -50.0, 200.0, -500.0, 150.0];
    let mut total_pnl = 0.0;

    for pnl in trades {
        // return - выходит из функции
        if pnl < -400.0 {
            return String::from("Критический убыток! Торговля остановлена.");
        }

        total_pnl += pnl;

        // break - выходит только из цикла
        if total_pnl >= 200.0 {
            println!("Дневная цель достигнута");
            break;
        }
    }

    format!("Итоговый PnL: ${:.2}", total_pnl)
}
```

## Поиск оптимальной точки входа

```rust
fn main() {
    let candles = [
        (42000.0, 42500.0, 41800.0, 42300.0),  // (open, high, low, close)
        (42300.0, 42400.0, 41900.0, 42000.0),
        (42000.0, 42100.0, 41500.0, 41600.0),  // Сильное падение
        (41600.0, 42000.0, 41400.0, 41900.0),
        (41900.0, 42200.0, 41800.0, 42100.0),
    ];

    let mut entry_candle: Option<usize> = None;

    for (i, &(open, high, low, close)) in candles.iter().enumerate() {
        let body_size = (close - open).abs();
        let lower_wick = open.min(close) - low;

        // Ищем "молот" — разворотную свечу
        if lower_wick > body_size * 2.0 && close > open {
            println!("Свеча #{}: Найден паттерн 'Молот'!", i + 1);
            entry_candle = Some(i);
            break;
        }

        println!("Свеча #{}: Нет сигнала", i + 1);
    }

    match entry_candle {
        Some(i) => println!("Рекомендация: вход после свечи #{}", i + 1),
        None => println!("Сигнал на вход не найден"),
    }
}
```

## Обработка потока ордеров

```rust
fn main() {
    let order_book = [
        ("BUY", 42000.0, 0.5),
        ("BUY", 41950.0, 1.0),
        ("SELL", 42100.0, 0.3),
        ("CANCEL", 0.0, 0.0),  // Сигнал остановки
        ("BUY", 42050.0, 0.8),  // Не будет обработан
    ];

    let mut total_buy_volume = 0.0;
    let mut total_sell_volume = 0.0;

    for (order_type, price, quantity) in order_book {
        if order_type == "CANCEL" {
            println!("Получен сигнал отмены. Остановка обработки.");
            break;
        }

        match order_type {
            "BUY" => {
                total_buy_volume += price * quantity;
                println!("BUY: {} @ ${:.2}", quantity, price);
            }
            "SELL" => {
                total_sell_volume += price * quantity;
                println!("SELL: {} @ ${:.2}", quantity, price);
            }
            _ => println!("Неизвестный тип ордера: {}", order_type),
        }
    }

    println!("\nИтого:");
    println!("Объём покупок: ${:.2}", total_buy_volume);
    println!("Объём продаж: ${:.2}", total_sell_volume);
}
```

## Практические упражнения

### Упражнение 1: Stop Loss мониторинг

```rust
fn main() {
    // Найди цену, при которой сработает stop loss
    let prices = [42000.0, 41800.0, 41500.0, 41200.0, 40800.0, 41000.0];
    let stop_loss = 41000.0;

    // Твой код здесь
    // Выведи цену срабатывания stop loss и индекс свечи
}
```

### Упражнение 2: Поиск разворота тренда

```rust
fn main() {
    // Найди первую свечу, где направление изменилось
    let closes = [100.0, 102.0, 104.0, 103.0, 101.0, 99.0];

    // Твой код здесь
    // Определи момент разворота (когда цена начала падать после роста)
}
```

### Упражнение 3: Лимит убытков

```rust
fn main() {
    // Остановись, когда суммарный убыток превысит $500
    let trades = [-100.0, 50.0, -200.0, 100.0, -300.0, -150.0, 200.0];

    // Твой код здесь
    // Выведи на какой сделке остановились и суммарный убыток
}
```

## Что мы узнали

| Концепция | Описание | Пример |
|-----------|----------|--------|
| `break` | Немедленный выход из цикла | `break;` |
| `break value` | Выход с возвратом значения | `break price;` |
| break vs return | break — из цикла, return — из функции | — |
| Условный break | Выход при выполнении условия | `if price >= tp { break; }` |

## Домашнее задание

1. **Trailing Stop**: Напиши функцию, которая мониторит цены и выходит, когда цена падает на 2% от максимума. Верни максимальную достигнутую цену и цену выхода.

2. **Поиск паттерна**: Реализуй поиск паттерна "три белых солдата" (три последовательные растущие свечи). Используй `break` для выхода при нахождении.

3. **Risk Manager**: Создай функцию, которая обрабатывает сделки и останавливается при:
   - Достижении дневного лимита прибыли ($1000)
   - Достижении дневного лимита убытков ($500)
   - Превышении максимального количества сделок (10)

   Верни причину остановки и итоговый PnL.

4. **Order Validator**: Напиши валидатор серии ордеров, который прекращает обработку при первом невалидном ордере и возвращает информацию об ошибке.

## Навигация

[← Предыдущий день](../022-loop-infinite-market/ru.md) | [Следующий день →](../024-continue-skip-trade/ru.md)
