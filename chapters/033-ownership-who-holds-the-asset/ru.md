# День 33: Ownership — Кто владеет активом?

## Аналогия из трейдинга

Представь, что ты владеешь акциями Tesla (TSLA). Эти акции принадлежат **тебе** — ты их настоящий владелец. Ты можешь:
- **Продать** их кому-то другому (передать владение)
- **Дать посмотреть** другу свой портфель (заимствование)
- **Уничтожить** позицию (закрыть)

Но что ты **не можешь** сделать:
- Продать одни и те же акции двум разным людям одновременно
- Использовать акции после того, как ты их продал

Именно так работает **Ownership** в Rust! Это система, которая гарантирует безопасность памяти без сборщика мусора.

## Три правила Ownership

```rust
fn main() {
    // Правило 1: У каждого значения есть владелец (owner)
    let portfolio = String::from("BTC: 1.5, ETH: 10.0");

    // Правило 2: В каждый момент времени может быть только ОДИН владелец
    // Правило 3: Когда владелец выходит из области видимости, значение удаляется

    println!("Портфель: {}", portfolio);
} // <- здесь portfolio уничтожается (drop)
```

## Move: Передача владения

Когда ты присваиваешь значение другой переменной, происходит **перемещение** (move):

```rust
fn main() {
    // Создаём ордер
    let order = String::from("BUY BTC 0.5 @ 42000");

    // Передаём владение ордером в новую переменную
    let executed_order = order;  // MOVE происходит здесь!

    // println!("{}", order);  // ОШИБКА! order больше не владеет данными
    println!("Исполнен: {}", executed_order);  // OK
}
```

**Аналогия:** Ты продал свои акции Tesla. После продажи они принадлежат новому владельцу — ты больше не можешь их использовать.

## Move vs Copy

Простые типы (числа, bool) копируются, а не перемещаются:

```rust
fn main() {
    // Числа КОПИРУЮТСЯ (Copy trait)
    let btc_price = 42000.0;
    let saved_price = btc_price;  // Копирование!

    println!("BTC сейчас: ${}", btc_price);      // OK!
    println!("Сохранённая цена: ${}", saved_price);  // OK!

    // String ПЕРЕМЕЩАЕТСЯ (нет Copy trait)
    let ticker = String::from("BTC/USDT");
    let saved_ticker = ticker;  // MOVE!

    // println!("{}", ticker);  // ОШИБКА!
    println!("Тикер: {}", saved_ticker);  // OK
}
```

**Почему так?**
- Числа маленькие и копируются быстро
- Строки могут быть огромными — копирование было бы дорогим

## Передача владения в функцию

Когда ты передаёшь значение в функцию, происходит move:

```rust
fn main() {
    let order = String::from("SELL ETH 5.0 @ 2800");

    execute_order(order);  // Владение передано в функцию

    // println!("{}", order);  // ОШИБКА! order больше не действителен
}

fn execute_order(order: String) {
    println!("Исполняем ордер: {}", order);
    // Здесь order уничтожается в конце функции
}
```

**Аналогия:** Ты передал ордер брокеру. После передачи ордер у брокера — ты им больше не управляешь.

## Возврат владения из функции

Функция может вернуть владение обратно:

```rust
fn main() {
    let order = String::from("BUY DOGE 1000 @ 0.15");

    // Передаём владение и получаем обратно
    let confirmed_order = process_order(order);

    println!("Подтверждённый ордер: {}", confirmed_order);
}

fn process_order(order: String) -> String {
    println!("Обработка: {}", order);
    // Возвращаем владение с модификацией
    format!("[CONFIRMED] {}", order)
}
```

## Практический пример: Управление портфелем

```rust
fn main() {
    // Создаём портфель
    let portfolio = create_portfolio();

    // Анализируем (передаём владение)
    let analyzed = analyze_portfolio(portfolio);

    // Выводим результат
    display_results(analyzed);

    // println!("{:?}", portfolio);  // ОШИБКА! portfolio уже перемещён
}

fn create_portfolio() -> String {
    String::from("BTC: 2.0, ETH: 15.0, SOL: 100.0")
}

fn analyze_portfolio(portfolio: String) -> String {
    let total_positions = portfolio.matches(',').count() + 1;
    format!("{} | Позиций: {}", portfolio, total_positions)
}

fn display_results(report: String) {
    println!("═══════════════════════════════════");
    println!("ОТЧЁТ ПО ПОРТФЕЛЮ");
    println!("═══════════════════════════════════");
    println!("{}", report);
    println!("═══════════════════════════════════");
}
```

## Практический пример: Обработка ордеров

```rust
fn main() {
    // Цепочка обработки ордера
    let order = create_order("BTC/USDT", "BUY", 0.5, 42000.0);
    let validated = validate_order(order);
    let executed = execute_order(validated);
    log_trade(executed);
}

fn create_order(symbol: &str, side: &str, qty: f64, price: f64) -> String {
    format!("{}|{}|{}|{}", symbol, side, qty, price)
}

fn validate_order(order: String) -> String {
    // Проверка ордера
    let parts: Vec<&str> = order.split('|').collect();
    if parts.len() == 4 {
        format!("[VALID] {}", order)
    } else {
        format!("[INVALID] {}", order)
    }
}

fn execute_order(order: String) -> String {
    if order.starts_with("[VALID]") {
        let order_id = 12345;  // В реальности — генерация ID
        format!("[EXECUTED #{}] {}", order_id, order)
    } else {
        format!("[REJECTED] {}", order)
    }
}

fn log_trade(trade: String) {
    println!("📝 Лог сделки: {}", trade);
}
```

## Практический пример: Расчёт риска

```rust
fn main() {
    let position = String::from("LONG BTC 1.5 @ 40000");

    // Расчёт стоп-лосса
    let (position, stop_loss) = calculate_stop_loss(position, 0.02);

    // Расчёт тейк-профита
    let (position, take_profit) = calculate_take_profit(position, 0.05);

    println!("Позиция: {}", position);
    println!("Stop-Loss: ${:.2}", stop_loss);
    println!("Take-Profit: ${:.2}", take_profit);
}

fn calculate_stop_loss(position: String, risk_percent: f64) -> (String, f64) {
    // Парсим цену из позиции
    let price: f64 = position
        .split('@')
        .last()
        .unwrap_or("0")
        .trim()
        .parse()
        .unwrap_or(0.0);

    let stop_loss = price * (1.0 - risk_percent);
    (position, stop_loss)  // Возвращаем владение обратно
}

fn calculate_take_profit(position: String, profit_percent: f64) -> (String, f64) {
    let price: f64 = position
        .split('@')
        .last()
        .unwrap_or("0")
        .trim()
        .parse()
        .unwrap_or(0.0);

    let take_profit = price * (1.0 + profit_percent);
    (position, take_profit)  // Возвращаем владение обратно
}
```

## Clone: Создание копии

Иногда нужно сохранить оригинал. Используй `.clone()`:

```rust
fn main() {
    let original_order = String::from("BUY ETH 10.0 @ 2500");

    // Создаём копию для бэкапа
    let backup = original_order.clone();

    // Отправляем оригинал на исполнение
    execute_order(original_order);

    // Бэкап всё ещё доступен!
    println!("Бэкап: {}", backup);
}

fn execute_order(order: String) {
    println!("Исполняем: {}", order);
}
```

**Внимание:** `.clone()` — это явное копирование данных. Оно может быть дорогим для больших структур!

## Типы с Copy trait

Эти типы копируются автоматически (без move):

```rust
fn main() {
    // Все эти типы имеют Copy trait
    let shares: i32 = 100;           // Количество акций
    let price: f64 = 42000.50;       // Цена
    let is_profitable: bool = true;  // Прибыльность
    let side: char = 'B';            // Buy/Sell

    // Передаём в функции — они копируются
    print_shares(shares);
    print_price(price);

    // Оригиналы всё ещё доступны!
    println!("Shares: {}, Price: ${}", shares, price);
}

fn print_shares(s: i32) {
    println!("Акций: {}", s);
}

fn print_price(p: f64) {
    println!("Цена: ${:.2}", p);
}
```

**Copy типы:**
- Все целые числа (`i8`, `i16`, `i32`, `i64`, `u8`, `u16`, `u32`, `u64`, `isize`, `usize`)
- Числа с плавающей точкой (`f32`, `f64`)
- Логический тип (`bool`)
- Символ (`char`)
- Кортежи из Copy типов: `(i32, f64)`

## Что мы узнали

| Концепция | Описание | Аналогия из трейдинга |
|-----------|----------|----------------------|
| Ownership | Каждое значение имеет владельца | Владение акциями |
| Move | Передача владения | Продажа актива |
| Drop | Удаление при выходе из scope | Закрытие позиции |
| Clone | Явное копирование | Создание дублирующего ордера |
| Copy | Автоматическое копирование | Числа в отчётах |

## Почему это важно для алготрейдинга?

1. **Безопасность памяти** — нет утечек памяти, нет dangling pointers
2. **Предсказуемость** — понятно, когда данные освобождаются
3. **Производительность** — нет сборщика мусора с паузами
4. **Надёжность** — компилятор ловит ошибки на этапе компиляции

```rust
fn main() {
    // Rust не даст тебе сделать такую ошибку:
    let order = String::from("BUY BTC");
    let order2 = order;

    // send_to_exchange(order);   // ОШИБКА компиляции!
    // send_to_backup(order);     // Ты не отправишь один ордер дважды!

    println!("Безопасный трейдинг с {}", order2);
}
```

## Упражнения

### Упражнение 1: Передача портфеля
Исправь код так, чтобы он компилировался:

```rust
fn main() {
    let portfolio = String::from("BTC: 1.0, ETH: 5.0");

    show_portfolio(portfolio);
    calculate_value(portfolio);  // Ошибка!
}

fn show_portfolio(p: String) {
    println!("Портфель: {}", p);
}

fn calculate_value(p: String) {
    println!("Расчёт стоимости для: {}", p);
}
```

### Упражнение 2: Цепочка обработки
Реализуй цепочку обработки ордера с правильной передачей владения:
1. `create_order()` → создаёт ордер
2. `add_timestamp()` → добавляет время
3. `sign_order()` → подписывает
4. `send_order()` → отправляет и печатает

### Упражнение 3: Clone vs Move
Определи, где нужен `.clone()`, а где можно обойтись без него:

```rust
fn main() {
    let ticker = String::from("AAPL");
    let price: f64 = 175.50;

    log_ticker(ticker);      // Нужен clone?
    log_price(price);        // Нужен clone?

    println!("{} @ ${}", ticker, price);  // Ошибка?
}
```

## Домашнее задание

1. **Система ордеров:** Создай функции для полного жизненного цикла ордера (создание → валидация → исполнение → логирование) с правильной передачей владения

2. **Портфолио менеджер:** Напиши программу, которая:
   - Создаёт портфель
   - Добавляет позиции (используя возврат владения)
   - Рассчитывает общую стоимость
   - Выводит отчёт

3. **Trade Logger:** Реализуй логгер сделок, где каждая сделка проходит через несколько этапов обработки, и на каждом этапе добавляется метаинформация

4. **Исследуй:** Что произойдёт, если попытаться использовать переменную после move? Какие сообщения выдаёт компилятор?

## Навигация

[← Предыдущий день](../032-statements-expressions/ru.md) | [Следующий день →](../034-references-borrowing/ru.md)
